---
images:
  - bg.jpg
---
<!DOCTYPE html>
<html>
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <link rel="stylesheet" href="{{ '/' | url }}style.css">
    </head>
    <body>
        <header>
          <nav>
            <ul class="nav">
              <li class="nav-item{% if nav.url == page.url %} nav-item-active{% endif %}">
                <a href="{{ '/' | url }}">Accueil</a>
              </li>
              <li class="nav-item{% if nav.url == page.url %} nav-item-active{% endif %}">
                <a href="{{ '/pages-jaunes' | url }}">Pages Jaunes</a>
              </li>
              <li class="nav-item{% if nav.url == page.url %} nav-item-active{% endif %}">
                <a href="{{ '/keebs' | url }}">Keebs</a>
              </li>
              <li class="nav-item{% if nav.url == page.url %} nav-item-active{% endif %}">
                <a href="{{ '/meetup' | url }}">Meetup</a>
              </li>
          </ul>
          </nav>
          {# <h1><a href="{{ '/' | url }}">{{ metadata.title }}</a></h1> #}
          <h2 class="big-title">MK<br>FR</h2>
          <div class="header-bg">
            <img src="img/bg.jpg" alt="">
          </div>
        </header>
        <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
          {{ content | safe }}
        </main>
        <footer></footer>
    </body>
</html>
